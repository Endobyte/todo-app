extends layout

block content
  h1 To Do Items
  table
    thead
      tr
        th Description
        th Completed?
        th Actions
    tbody
      each item in toDoItems
        tr
          td= item.description
          td= item.completed
          td
            div.row
              if item.completed
                a.pressme(href=`incomplete/${item.id}`) Mark Incomplete
              else
                a.pressme(href=`complete/${item.id}`) Mark Complete
              a.pressme(href=`delete/${item.id}`) Delete Item
              a.pressme(href=`edit/${item.id}`) Edit Item

  a(href="/add") Add Item